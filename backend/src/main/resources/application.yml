spring:
  application:
    name: paylens-backend
  datasource:
    url: jdbc:h2:mem:paylens;DB_CLOSE_DELAY=-1
  jpa:
    hibernate:
      ddl-auto: update

server:
  port: 8080

paylens:
  intent-agent:
    base-url: ${PAYLENS_INTENT_AGENT_URL:http://localhost:8000}
